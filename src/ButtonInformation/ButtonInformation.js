// ButtonInformation.js
import React from 'react';

const ButtonInformation = ({ onClick }) => (
    <button className="show-full-info-btn" onClick={onClick}>
        View More
    </button>
);

export default ButtonInformation;
